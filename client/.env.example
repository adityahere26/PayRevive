# payrevive — frontend environment configuration (Vite)
# Copy to .env.local for local development. Never put secrets here — anything prefixed
# VITE_ is bundled into the browser build and is publicly visible (SECURITY.md § Frontend
# security). This should only ever hold the backend's public base URL.

VITE_API_BASE_URL=http://localhost:4000/api
