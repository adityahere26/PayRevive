import PagePlaceholder from "../components/PagePlaceholder.jsx";

export default function AuditTrail() {
  return (
    <PagePlaceholder
      title="Audit Trail"
      phase="Planned — Day 3+"
      description="Will show the full audit_logs trail per recovery case (AGENT_DESIGN.md § Audit Logger) — every detection, decision, policy check, and action, in order."
    />
  );
}
