import PagePlaceholder from "../components/PagePlaceholder.jsx";

export default function Evaluation() {
  return (
    <PagePlaceholder
      title="Evaluation — Synthetic Data"
      phase="Planned — Day 6"
      description="Will run the batch evaluation engine over synthetic cases and report recovered revenue, recovery rate, and policy metrics (EVALUATION.md). Always clearly labeled as synthetic, never real Razorpay activity."
    />
  );
}
