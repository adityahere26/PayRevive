import PagePlaceholder from "../components/PagePlaceholder.jsx";

export default function MerchantPolicy() {
  return (
    <PagePlaceholder
      title="Merchant Policy"
      phase="Planned — Day 6"
      description="Will let the merchant configure MAX_RECOVERY_ATTEMPTS, MAX_VOICE_ATTEMPTS, MAX_AUTONOMOUS_AMOUNT, RECOVERY_WINDOW_HOURS, and opt-out behavior (RECOVERY_POLICY.md § Merchant policy fields)."
    />
  );
}
